# 参数注解速查（@RequestParam / @Param / @PathVariable / @RequestBody）

> @RequestParam 和 @Param 名字像，但**属于两个框架、工作在两层、解决两件事**：
> Controller 层（Spring MVC）管"HTTP → Java"，Mapper 层（MyBatis）管"Java → SQL"。
> 没有叫 @Params 的注解。

## 1. 一条请求的完整旅程（先看各守哪道门）

```
DELETE /system/basic/pos/?ids=1&ids=2
   │
   ▼
① @RequestParam（或不写注解的数组）—— HTTP 参数收进 Controller   ← Spring MVC 的地盘
   │
   ▼
② controller 调 service、service 调 mapper —— 普通 Java 传参，没有注解的事
   │
   ▼
③ @Param("ids") —— 给参数命名，XML 的 collection="ids" 靠它找到值  ← MyBatis 的地盘
   │
   ▼
④ #{id} 占位符填值，SQL 发给数据库
```

### 报错速查（按这条链定位）

| 报错 | 炸在哪 | 原因 |
|---|---|---|
| `No primary or single unique constructor found for interface java.util.List` | ① Controller | 方法参数是裸 List，Spring 把它当表单模型去 new，List 是接口没构造器 |
| `The expression 'xxx' evaluated to a null value` | ③ Mapper | XML 里 `collection="xxx"` 的名字和 @Param 值（或默认 key）对不上，取出来是 null |

## 2. @RequestParam（Spring MVC，Controller 方法参数上）

把 HTTP 请求参数（查询串 `?xxx=` 或表单字段）绑定进方法参数。

| 属性 | 干什么 |
|---|---|
| `name` / `value` | HTTP 参数名和 Java 参数名不一致时映射：`@RequestParam("keywords") String kw` |
| `required` | 默认 `true`，没传报 400；设 `false` 则没传时是 null |
| `defaultValue` | 没传时的默认值（设了它 required 自动变 false）。分页不传 page 按第 1 页，靠它 |

```java
// 收 ?ids=1&ids=2（同一参数名重复出现）
public RespBean deletePositionsByIds(@RequestParam Long[] ids) { ... }

// 分页默认值（阶段 6 员工列表）
public RespBean getEmployeeByPage(@RequestParam(defaultValue = "1") Integer page,
                                  @RequestParam(defaultValue = "10") Integer size) { ... }
```

### 不写注解行不行（关键规则）

- **简单类型 + 它们的数组**：不写注解默认就按请求参数绑定 → vhr 的 `deletePositionsByIds(Integer[] ids)` 不写注解能跑
- **List/集合**：裸写必炸（见报错速查第 1 行）；要么加 `@RequestParam`，要么改用数组
- 要用 `defaultValue` 等属性时，必须显式写

## 3. Controller 层三兄弟（按"值在哪"选）

| 注解 | 取值来源 | 项目里的例子 |
|---|---|---|
| `@PathVariable` | 路径变量 `/{id}` | `deleteByPrimaryKey(@PathVariable Long id)` |
| `@RequestParam` | 查询串/表单 `?ids=1` | 批量删除的 ids |
| `@RequestBody` | 请求体 JSON | `addPosition(@RequestBody Position position)` |

## 4. @Param（MyBatis，Mapper 接口方法参数上）

给参数起名字，让 XML 的 `#{}`、`collection`、`test` 能按名字引用。

**为什么需要**：Java 编译后反射拿不到参数名，@Param 是手工把名字补回去，不依赖任何编译条件。

### 名字规则（XML 里用哪个名字取参数）

1. 加了 `@Param("ids")` → key 就是 `ids`，XML 必须写 `ids`
2. 不加 @Param 的单个**集合/数组** → key 固定：List → `list`（或 `collection`），数组 → `array`
3. 不加 @Param 的单个**普通参数**（对象/String/Long）→ `#{}` 里名字随便写都能取到
4. （现代机制）`-parameters` 编译开关 + MyBatis 3.4.1+ 默认 `useActualParamName`，真实参数名可直接用——但依赖编译开关生效，静默失效时报的就是 null 那个错；@Param 永远可靠

**多参数方法**必须 @Param（或依赖第 4 条），否则 XML 里 `#{page}` 找不到参数：

```java
List<Employee> getEmployeeByPage(@Param("page") Integer page,
                                 @Param("size") Integer size,
                                 @Param("emp") Employee emp,
                                 @Param("beginDateScope") Date[] beginDateScope);
// XML 对应：#{page}、#{size}、#{emp.name}、limit #{page}, #{size}
```

### foreach 里的名字对照

- `@Param("ids")` ↔ `collection="ids"`：**必须配对**（collection 是"从参数 Map 里取哪个值"）
- `item="id"`：循环内**局部变量名，自定义**，循环体里 `#{id}` 引用
- vhr 原版全家桶：Controller `Integer[] ids` → Service `Integer[] ids` → Mapper `@Param("ids") Integer[] ids` → XML `collection="ids"` + `#{id}`（myhr 的 id 是 Long，所以全程 Long[]）

## 5. 案例复盘：批量删除两连炸（2026-09-23）

1. **Controller 裸 List**：`deletePositionsByIds(List<Long> idList)` → `No ... constructor found for interface java.util.List`。Spring 对无注解参数的规则：简单类型及其数组按请求参数绑定，其他（含 List）当表单模型去实例化。改数组 `Long[] ids` 过关（vuehr 前端发的是 `?ids=1&ids=2&` 查询串，PosMana.vue 拼的）。
2. **collection 名字对不上**：接口 `Long[] idList` 没加 @Param，XML 写 `collection="idList"` → `The expression 'idList' evaluated to a null value`。参数 Map 里实际只有默认 key `array`，Java 方法参数名 MyBatis 根本不用。改 `@Param("ids") Long[] ids` + `collection="ids"` 过关。

记法：**报错认层**——构造器错是 Spring 绑定层，null 值错是 MyBatis 命名层，SQL 语法错才是最后发语句那层。每过一关，错误就换一副面孔。
