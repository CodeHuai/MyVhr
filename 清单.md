# 清单

> 方法：读 vhr 对应文件 → 合上源码，在 myhr 写同名类/方法/接口 → 自测 → commit。
> 每个步骤下面带一行"为什么"，说明这一步的目的；实现细节仍以 vhr 源码为准。

## 位置说明

- vhr 源码在 `D:\PersonCodeDir\java\vhr\vhr\vhrserver` 下，分 4 个模块：
  - **vhr-web**：控制器、安全配置
  - **vhr-service**：业务逻辑
  - **vhr-mapper**：Mapper 接口和 XML
  - **vhr-model**：实体类
- myhr 的 4 个模块一一对应：myhr-web、myhr-service、myhr-mapper、myhr-model
- vhr 的类在 `org.javaboy.vhr` 包下，myhr 在 `com.myhr` 包下，包结构相同
- vuehr 前端：`D:\PersonCodeDir\java\vhr\vuehr`
- 数据库 myvhr；后端端口 10002；前端端口 10086；账号 admin / 123456

---

## 阶段 0：环境与数据准备

- [x] 1. 启动 MySQL、Redis、MyhrApplication，`curl http://localhost:10002/ping` → `{"status":200,"msg":"pong"}`
  - 为什么：先确认骨架能跑、两个中间件连通，后面每个阶段都依赖它们，出问题先排除环境因素。
- [ ] 2. 数据验证：
  ```sql
  USE myvhr;
  SELECT username, enabled FROM hr;
  SHOW PROCEDURE STATUS WHERE Db = 'myvhr';   -- 应有 addDep、deleteDep
  SELECT COUNT(*) FROM menu;
  SELECT COUNT(*) FROM employee;
  ```
  - 为什么：addDep/deleteDep 两个存储过程是阶段 3 部门增删要调的，导入工具可能把它们丢了，先确认在。
- [x] 3. `redis-cli -a 123456 ping` → PONG
  - 为什么：阶段 2 的菜单缓存（menus_cache）存在 Redis 里，连不上后面会报错。
- [x] 4. vuehr：`vue.config.js` 里两处 proxy target（ws 和 http）改成 10002 → `npm install && npm run serve` → 打开 `http://localhost:10086`
  - 为什么：proxy 默认指向 vhr 原后端（10001），不改的话你联调的一直是原版，不是 myhr。
- [x] 5. commit：`阶段0：环境与数据准备`

## 阶段 1：登录与认证

先看 vhr 这些文件：
- vhr-web：config/LoginFilter.java、config/SecurityConfig.java、config/VerificationCode.java、controller/LoginController.java
- vhr-service：HrService.java
- vhr-model：Hr.java、Role.java
- vhr-mapper：HrMapper.java、HrMapper.xml

**注意：vhr 的 SecurityConfig 里有 3 处是阶段 4 的内容——`@Autowired CustomFilterInvocationSecurityMetadataSource`、`@Autowired CustomUrlDecisionManager` 两个字段，和 configure(http) 里 withObjectPostProcessor 那一段。这两个类现在还没写，照抄会编译不过，先删掉不写，阶段 4 步骤 8 再补回来。**

- [x] 1. model 模块：新建 `Role` 实体（id / name / nameZh + getter/setter）
  - 为什么：登录时要把用户的角色一并查出来，Hr 里要挂 List\<Role\>；nameZh 是角色的中文名（页面显示用），name 是给 Spring Security 用的编码（ROLE_xxx）。
- [x] 2. model 模块：重写 `Hr`：
   - 10 个字段：id、name、phone、telephone、address、enabled、username、password、userface、remark（类型照 vhr），外加 `List<Role> roles`
   - 全部 getter/setter
   - `implements UserDetails`，覆写 7 个方法：
     - `getAuthorities()`：遍历 roles，每个 new SimpleGrantedAuthority(role.getName()) 装进 List 返回，方法上加 @JsonIgnore
     - `getPassword()`、`getUsername()`：普通 getter
     - `isAccountNonExpired()`、`isAccountNonLocked()`、`isCredentialsNonExpired()`：return true
     - `isEnabled()`：return enabled
   - equals / hashCode：照 vhr，只比较 username
  - 为什么：`UserDetailsService.loadUserByUsername` 的返回类型是 UserDetails，Hr 实现了这个接口就能直接当返回值，认证通过后整个 Hr 存进 session，之后到处能 `((Hr) principal)` 强转拿业务字段。
  - 为什么 getAuthorities 包的是 role.getName()：鉴权时比对的两边必须是同一种东西——接口需要什么角色来自 menu→role.name，所以用户这边也要交 role.name（包 username 永远对不上）。@JsonIgnore 是因为这是个算出来的方法不是字段，不加 JSON 里会多一个 authorities 节点。
  - 为什么四个状态方法三个 return true：hr 表只有 enabled 一个状态字段，接口逼你回答四个问题，没有的业务就答"没问题"；isEnabled 返回真实字段，所以"禁用操作员"后那人登录会收到"账户被禁用"。
  - 为什么 equals/hashCode 只比 username：SessionRegistry 靠 principal 的 equals 识别"这是同一个人"来顶号，登录成功后 password 会被置 null，全字段比较会导致同一个人前后两个对象不相等。
- [x] 3. mapper 模块：重写 `HrMapper.xml`：
   - resultMap：`BaseResultMap`，type=Hr，10 列逐一映射
   - `loadUserByUsername`：单表 `select * from hr where username=#{username}`，用 BaseResultMap
   - 新增 `getHrRolesById`：`select r.* from role r, hr_role hrr where hrr.rid=r.id and hrr.hrid=#{id}`，resultType 用 Role
   - `HrMapper.java` 接口加这两个方法（参数 @Param）
  - 为什么：认证链第一步只要"这个人"（loadUserByUsername 单表就够）；角色是认证通过前后另查一次挂上去，两次查询各干一件事。注意条件是 hrr.**hrid** 不是 hrr.id——id 是中间表自己的主键，hrid 才指向 hr 表。
- [x] 4. service 模块：重写 `HrService`：
   - `implements UserDetailsService`
   - 覆写 `loadUserByUsername(String username)`：调 hrMapper.loadUserByUsername，查不到抛 `UsernameNotFoundException("用户名不存在!")`；查到先 `hr.setRoles(hrMapper.getHrRolesById(hr.getId()))` 再 return hr
  - 为什么：SecurityConfig 里 `auth.userDetailsService(hrService)` 注册的就是它——Spring Security 不管用户存在哪张表，认证时回调这个方法查用户，这是后端和框架之间唯一的用户数据接入口。roles 必须在返回前塞好，getAuthorities 才有数据。
- [x] 5. web 模块：新建 `VerificationCode`（config 包）：
   - 宽 100 高 30、4 位随机字符、5 条干扰线
   - 三个公开方法：`getImage()`（返回 BufferedImage）、`getText()`（返回验证码文字）、`static output(image, OutputStream)`（JPEG 写出）
  - 为什么：图片验证码防脚本自动爆破；干扰线和随机字体防简单 OCR 识别。
- [x] 6. web 模块：新建 `LoginController`（controller 包）：
   - `GET /login`：return RespBean.error("尚未登录，请登录!")
   - `GET /verifyCode`：new VerificationCode → getText 存 `request.getSession(true).setAttribute("verify_code", text)` → output 写到 resp.getOutputStream()
  - 为什么：验证码文字存 session，是给 LoginFilter 校验用的（用户看图填码 → 提交时和 session 里存的比对）；/login 是给前端请求失败时拿提示文案用的。
- [x] 7. web 模块：新建 `LoginFilter`（config 包）：
   - 7.1 `extends UsernamePasswordAuthenticationFilter`，类上不加任何注解（靠 SecurityConfig 的 @Bean 方法注册），不需要 SessionRegistry 字段
   - 7.2 覆写 `attemptAuthentication(request, response)`，方法体按顺序做 5 件事：
     - 1. 不是 POST → throw new AuthenticationServiceException(...)
     - 2. 取 session 验证码：`String verify_code = (String) request.getSession().getAttribute("verify_code")`
     - 3. Content-Type 含 application/json → 走 JSON 分支；否则走表单分支
     - 4. JSON 分支：ObjectMapper 把 request.getInputStream() 读成 Map → 取 code 先 checkCode → 取 username/password（null 补 ""，username trim）→ new UsernamePasswordAuthenticationToken(username, password) → setDetails → return this.getAuthenticationManager().authenticate(authRequest)
     - 5. 表单分支：checkCode(response, request.getParameter("code"), verify_code) → return super.attemptAuthentication(request, response)
   - 7.3 单独写 `checkCode(HttpServletResponse resp, String code, String verify_code)`：忽略大小写比对，不符抛 AuthenticationServiceException("验证码不正确")
  - 为什么自定义过滤器：Spring Security 默认表单登录只认 form 提交的 username/password，vuehr 发的是 JSON（还带验证码字段），默认的接不住。
  - 为什么 /doLogin 不写在 Controller 里：过滤器在 DispatcherServlet 之前执行，LoginFilter 用 setFilterProcessesUrl("/doLogin") 直接拦截这个路径，根本到不了 Controller。
  - 为什么验证码在认证之前校验：错验证码直接抛异常结束，不浪费一次查库和密码比对。
  - ⚠️ 与 vhr 的差异：vhr 在这一步手动调 sessionRegistry.registerNewSession（认证前注册），会把当前会话提前算进名额，顶号时连自己一起踢（现象：第二个浏览器登录后进不了首页）。myhr 已删掉这句，注册改由 8.7 的 RegisterSessionAuthenticationStrategy 在认证成功后做。
- [x] 8. web 模块：重写 `SecurityConfig`（config 包），按顺序搭：
   - 8.1 类骨架：@Configuration + extends WebSecurityConfigurerAdapter
   - 8.2 @Autowired HrService
   - 8.3 `@Bean PasswordEncoder passwordEncoder()`：return new BCryptPasswordEncoder()
     - 为什么：数据库密码存的是 BCrypt 密文（每次加密结果都不同），比对必须用同一个 encoder 的 matches 方法，不能字符串相等。
   - 8.4 覆写 `configure(AuthenticationManagerBuilder auth)`：`auth.userDetailsService(hrService)`
     - 为什么：告诉认证链"去 HrService 查用户"。不配这个，框架不知道你的用户数据在哪。
   - 8.5 覆写 `configure(WebSecurity web)`：`web.ignoring().antMatchers("/css/**", "/js/**", "/index.html", "/img/**", "/fonts/**", "/favicon.ico", "/verifyCode")`
     - 为什么：/verifyCode 必须放行——它在登录之前请求，不放行就变成"先登录才能拿验证码"的死循环；静态资源同理直接走。
   - 8.6 `@Bean SessionRegistryImpl sessionRegistry()`：return new SessionRegistryImpl()
     - 为什么：做成单例 Bean，RegisterSessionAuthenticationStrategy（登记会话）和 ConcurrentSessionFilter（检查会话是否被顶）共享同一份登记表才能配合。
   - 8.7 `@Bean LoginFilter loginFilter() throws Exception`，依次：
     - new LoginFilter()
     - setAuthenticationSuccessHandler：JSON 输出 RespBean.ok("登录成功!", hr)，输出前 hr.setPassword(null)
       - 为什么置空密码：密文没必要（也不应该）回传给前端。
     - setAuthenticationFailureHandler：JSON 输出，按异常类型分提示——LockedException→"账户被锁定，请联系管理员!"、CredentialsExpiredException→"密码过期，请联系管理员!"、AccountExpiredException→"账户过期，请联系管理员!"、DisabledException→"账户被禁用，请联系管理员!"、BadCredentialsException→"用户名或者密码输入错误，请重新输入!"
       - 为什么：这些异常正是 Hr 里那四个状态方法+密码比对失败抛出来的，在这里翻译成人话。
     - setAuthenticationManager(authenticationManagerBean())
     - setFilterProcessesUrl("/doLogin")
     - 组合两个会话策略挂上去：new ConcurrentSessionControlAuthenticationStrategy(sessionRegistry()) + setMaximumSessions(1)（数会话数、踢超限的旧会话）；new RegisterSessionAuthenticationStrategy(sessionRegistry())（认证成功后登记当前会话）；用 new CompositeSessionAuthenticationStrategy(Arrays.asList(前两个)) 再 setSessionAuthenticationStrategy
       - 为什么 maximumSessions=1：同一账号最多 1 个会话，第二次登录触发顶号。
       - 为什么用组合策略（与 vhr 的差异）：vhr 只挂了数数踢人的策略，靠 LoginFilter 在认证前手动注册会话，时机太早导致当前会话被多算一个、顶号时连自己一起踢；RegisterSession 在认证成功后才注册，时机才对。
     - return loginFilter
   - 8.8 覆写 `configure(HttpSecurity http)`，链式写：
     - http.authorizeRequests().anyRequest().authenticated()
     - .and().logout().logoutSuccessHandler(JSON 输出 RespBean.ok("注销成功!")).permitAll()
     - .and().csrf().disable().exceptionHandling().authenticationEntryPoint(输出 401 + JSON：先 RespBean.error("访问失败!")，若 authException 是 InsufficientAuthenticationException 则 setMsg("请求失败，请联系管理员!"))
       - 为什么必须改 entryPoint：未认证时 Spring Security 默认重定向到登录页——那是传统 JSP 做法；前后端分离下前端收到重定向根本没法处理，必须改回 401+JSON 让前端自己跳登录页。
     - http.addFilterAt(new ConcurrentSessionFilter(sessionRegistry(), event → 输出 401 + JSON "您已在另一台设备登录，本次登录已下线!"), ConcurrentSessionFilter.class)
       - 为什么：每个请求都过它一遍，检查这个会话是不是已被顶下线，被顶了就返回这句话。
     - http.addFilterAt(loginFilter(), UsernamePasswordAuthenticationFilter.class)
       - 为什么 addFilterAt：在默认表单登录过滤器的位置替换成自己的，同一个位置只能有一个。
   - 删掉骨架里原来的 formLogin() 那套
- [x] 9. 自测：验证码显示/刷新；错验证码→"验证码不正确"；错密码→"用户名或者密码输入错误"；admin/123456 登录进主页面（侧边栏空、菜单接口报错正常，阶段 2 做）；双浏览器同账号顶号下线；注销后再请求任意接口 401
- [x] 10. commit：`阶段1：登录与认证`（9139ae6，顶号修复另见 34ae39d）

## 阶段 2：动态菜单

先看 vhr 这些文件：
- vhr-web：controller/config/SystemConfigController.java
- vhr-service：MenuService.java
- vhr-model：Menu.java、Meta.java
- vhr-mapper：MenuMapper.java、MenuMapper.xml

- [x] 1. model 模块：新建 `Meta`（keepAlive / requireAuth）
  - 为什么：menu 表里这两列是给前端路由用的开关（页面是否缓存、是否需要登录），单独抽成对象跟着 menu 走。
- [x] 2. model 模块：新建 `Menu`：字段 id、url、path、component、name、iconCls、parentId、enabled + Meta meta + List\<Menu\> children + List\<Role\> roles，全 getter/setter
  - 为什么：url 是后端接口路径（阶段 4 鉴权靠它匹配）；path/component/name/iconCls 是前端路由的四个要素；children 是树形结构的载体；roles 本阶段用不上，阶段 4 的 getAllMenusWithRole 要用，先建好。
- [x] 3. mapper 模块：新建 `MenuMapper.xml`：
   - `BaseResultMap`：8 列映射（id、url、path、component、name、iconCls、parentId、enabled）+ association 映射 meta（keepAlive、requireAuth 两列）
   - `Menus2`（extends BaseResultMap）：collection 映射 children；vhr 的 SQL 给子菜单起的别名是 id2、component2、enabled2、iconCls2、keepAlive2、name2、parentId2、requireAuth2、path2（注意 vhr 没选 url2，对照源码写）
   - `getMenusByHrId`：五表联查（menu m1, menu m2, hr_role hrr, menu_role mr），条件 m1.id=m2.parentId、hrr.hrid=#{hrid}、hrr.rid=mr.rid、mr.mid=m2.id、m2.enabled=true，select distinct，子菜单列起别名，order by m1.id, m2.id
   - `MenuMapper.java` 接口加方法
  - 为什么子菜单列要起别名：父子两行记录的列名相同，resultMap 靠列名对号入座，不起别名（id2/...）子菜单数据会盖掉父菜单的。
  - 为什么 distinct：一个菜单可能授权给了用户的多个角色，联查会出重复行。
  - 为什么只查两级：用户可见的菜单就是"一级分组 + 二级菜单"两层，层级靠 m1.id=m2.parentId 一关联就带出来了；m2.enabled=true 把停用的菜单剔掉。
  - 为什么 mr.mid 关联的是 m2 不是 m1：menu_role 里只存叶子菜单的 id（权限管理保存时 `getCheckedKeys(true)` 只收集叶子节点），一级菜单不直接记权限，它的可见性是被 m1.id=m2.parentId 从子菜单推导出来的，换成 m1.id 永远配不上 → 空结果。详见 `menu_role只存叶子菜单id.md`
- [x] 4. service 模块：新建 `MenuService`：类上 @CacheConfig(cacheNames = "menus_cache")；方法 `getMenusByHrId()`：从 SecurityContextHolder.getContext().getAuthentication().getPrincipal() 强转 Hr，取 id 调 mapper
  - 为什么方法无参、从 SecurityContextHolder 拿人：登录后 Hr 就在安全上下文里，前端不用传、也传不得（传 id 就能查别人的菜单了）。
  - ⚠️ 补漏：类上的 @CacheConfig(cacheNames = "menus_cache") 尚未加（vhr 原文第 27 行有），阶段 4 的 @Cacheable 靠它指定缓存名，补上后再补一个 commit。
- [x] 5. web 模块：新建 `SystemConfigController`（controller/config 包）：`GET /system/config/menu` 返回 List\<Menu\>
  - 为什么：前端登录成功后立刻调这个接口渲染侧边栏，这是菜单的唯一出口。
- [x] 6. 自测：登录后 admin 见全部菜单；`UPDATE menu SET enabled=0 WHERE id=xx` 后重新登录该菜单消失
- [x] 7. commit：`阶段2：动态菜单`（b852366；补上步骤 4 的 @CacheConfig 后再补一个 commit）

## 阶段 3：基础信息管理

先看 vhr 这些文件：
- vhr-web：controller/system/basic/PositionController.java、JobLevelController.java、DepartmentController.java；exception/GlobalExceptionHandler.java
- vhr-service：PositionService.java、JobLevelService.java、DepartmentService.java
- vhr-model：Position.java、JobLevel.java、Department.java、RespPageBean.java
- vhr-mapper：PositionMapper、JobLevelMapper、DepartmentMapper（各一个 .java + .xml）

- [x] 1. model 模块：新建四个实体：
   - `Position`（id / name / createDate / enabled）
   - `JobLevel`（id / name / titleLevel / createDate / enabled）
   - `Department`（id / name / parentId / depPath / enabled / isParent + List\<Department\> children + Integer result）
   - `RespPageBean`（Long total + List<?> data）
  - 为什么 Department 有个非表字段 result：部门增删走存储过程，result 存过程的 OUT 返回值（成功/有员工/有子部门），MyBatis 会把 OUT 参数写回这个字段。
  - 为什么先建 RespPageBean：分页接口的统一返回结构（总数+当页数据），阶段 6 员工列表开始大量使用，先放进 model 备用。
- [x] 2. 职位-列表：mapper 写 `getAllPositions`（resultMap 四列映射）→ service 写 getAllPositions → web 写 `GET /system/basic/pos/` 返回 List
  - 为什么不分页：职位就几十条，一次全查，前端自己过滤；vhr 只在数据量大的员工表做分页。
- [x] 3. 职位-新增：mapper 写 `insertSelective`（trim + if 动态列）→ service 写 addPosition（setEnabled(true)、setCreateDate(new Date())，返回影响行数）→ web 写 `POST /`，==1 返回"添加成功!"（阶段性 commit 7364bdb）
  - 为什么 enabled/createDate 在 Service 里设而不是靠前端传：默认值是后端的规矩，前端传什么都不算数。
  - 为什么用 insertSelective 不用 insert：selective 只拼非空字段，调用方少传字段也不报错。
- [x] 4. 职位-编辑：mapper 写 `updateByPrimaryKeySelective`（set + if）→ service 写 updatePositions → web 写 `PUT /`
  - 为什么 selective：编辑时只更新传了的字段（比如只改启用状态），其他列不动。
  - 已写完（随阶段 3 一起 commit）：myhr 的 service/controller 方法名实际用了 updateByPrimaryKeySelective，vhr 原版叫 updatePositions。
- [x] 5. 职位-单个删除：mapper 写 `deleteByPrimaryKey` → service 写 deletePositionById → web 写 `DELETE /{id}`
  - 已写完（随阶段 3 一起 commit）：myhr 的 service/controller 方法名实际用了 deleteByPrimaryKey，vhr 原版叫 deletePositionById；参数是 Long。
- [ ] 6. 职位-批量删除：mapper 写 `deletePositionsByIds`（`delete from position where id in <foreach>`）→ service → web 写 `DELETE /`（参数 Integer[] ids），==ids.length 成功
  - 为什么 foreach 拼 in：一条 SQL 删多行；循环调单删要开 N 次数据库往返。
  - ⚠️ 进行中（2026-09-23）：三层代码都写了，对照 vhr 原版还差这几处：
    - XML：`delete from positon and id in` → 表名拼错（应为 position）；去掉 1=1 时把 where 一起带掉了，连接词应为 where
    - XML：`${id}` → `#{id}`
    - 接口：vhr 原版是 `Integer deletePositionsByIds(@Param("ids") Integer[] ids)` 配 `collection="ids"`；myhr 接口没加 @Param，XML 的 `collection="idList"` 取不到值——要么接口加 `@Param("idList")`，要么 collection 改成 `"list"`
    - 类型差异：myhr 的 id 是 Long（vhr 是 Integer，jdbcType 也随之用 BIGINT），controller/service 传的是 List\<Long\>
- [ ] 7. 职称：照 2~6 重复一遍（接口路径 `/system/basic/joblevel`，方法名对照 JobLevelController / JobLevelService）
  - 为什么重复写一遍：CRUD 套路练的就是熟练度，第二遍尽量不看第一遍的代码，写完再对照。
- [ ] 8. 部门-树查询：mapper 写 `DepartmentWithChildren` resultMap（collection 的 select 属性指向自身 getAllDepartmentsByParentId，column="id"）+ `getAllDepartmentsByParentId(pid)` 查询 → service 写 getAllDepartments（调 mapper 传 -1）→ web 写 `GET /system/basic/department/`
  - 为什么用嵌套 select 递归：部门层级不定深（总监→经理→主管→...），"查父 id → 每个父再查一层子"的写法天然支持任意深度，不用写死几层。
  - 为什么入口传 -1：根部门的 parentId 存的是 -1，从 -1 开始顺着 children 一层层递归下去。
- [ ] 9. 部门-新增：mapper 写 `addDep`（statementType="CALLABLE"，`call addDep(...)`，name/parentId/enabled 是 IN，result/id 是 OUT）→ service 写 addDep（setEnabled(true) 后调 mapper）→ web 写 `POST /`（dep.getResult()==1 → "添加成功"）
  - 为什么走存储过程：加一个部门不只是 insert 一行——父部门的 isParent 要改、depPath 路径串要维护，多条 SQL 的原子性放数据库端一个过程里做最省事（过程已在库里）。
- [ ] 10. 部门-删除：mapper 写 `deleteDepById`（CALLABLE，`call deleteDep(id, result)`）→ service 写 deleteDepById → web 写 `DELETE /{id}`（-2→"该部门下有子部门"；-1→"该部门下有员工"；1→"删除成功"）
  - 为什么三种返回值：删除前要先在过程里检查"有没有子部门/有没有员工"，检查逻辑在数据库端，Java 这边只负责把数字翻译成提示。
- [ ] 11. web 模块：新建 `GlobalExceptionHandler`（exception 包）：@RestControllerAdvice；@ExceptionHandler(SQLException.class) 的方法：instanceof SQLIntegrityConstraintViolationException → "该数据有关联数据，操作失败!"，否则"数据库异常，操作失败!"
  - 为什么：删一个"还有员工在用"的职位，数据库直接抛外键异常，没有这层兜底前端收到的是 500 白板；@RestControllerAdvice 会拦下所有 Controller 抛出的同类异常统一转成 RespBean。
- [ ] 12. 自测：职位/职称增删改、批量删、启用禁用；删被引用职位弹"该数据有关联数据"；部门树展示、加子部门、三种删除结果
- [ ] 13. commit：`阶段3：基础信息管理`

## 阶段 4：权限组 + 动态鉴权

先看 vhr 这些文件：
- vhr-web：controller/system/basic/PermissController.java、config/CustomFilterInvocationSecurityMetadataSource.java、config/CustomUrlDecisionManager.java
- vhr-service：RoleService.java、MenuService.java
- vhr-mapper：MenuMapper.xml、MenuRoleMapper.java/.xml、RoleMapper.java/.xml

- [ ] 1. mapper + service：`RoleMapper`（BaseResultMap + getAllRoles / insert / deleteByPrimaryKey）→ `RoleService`：getAllRoles、addRole（name 不以 "ROLE_" 开头就 setName("ROLE_"+name) 再 insert）、deleteRoleById
  - 为什么自动补 ROLE_ 前缀：Hr.getAuthorities() 返回的就是角色的 name 字符串，鉴权比对两边要一致；界面上录入的角色名五花八门，后端统一保证带前缀。
- [ ] 2. mapper 模块：`MenuMapper.xml` 补三个查询 + 接口同步：
   - `MenuWithChildren` resultMap（三层嵌套 collection，列别名 id1/name1、id2/name2、id3/name3）+ `getAllMenus`（menu 三层自连接 m1→m2→m3，m3.enabled=true）
     - 为什么三层：权限组页面要展示完整的菜单树供勾选，库里菜单最深三层，一次自连接全带出来。
   - `MenuWithRole` resultMap（collection 映射 roles，列别名 rid/rname/rnameZh）+ `getAllMenusWithRole`（menu、menu_role、role 三表联查）
   - `getMidsByRid`：`select mid from menu_role where rid=#{rid}`
     - 为什么：编辑角色时回显"这个角色已经勾了哪些菜单"，前端拿 mids 数组去勾选框打勾。
- [ ] 3. service 模块：`MenuService` 补方法：`getAllMenusWithRole` 加 @Cacheable、getAllMenus、getMidsByRid、`updateMenuRole(rid, mids)` 加 @Transactional（先 menuRoleMapper.deleteByRid(rid)，mids 空直接 return true，再 insertRecord）
  - 为什么 @Cacheable 加在 getAllMenusWithRole：动态鉴权每个请求都要调它查"每个 url 需要什么角色"，读得极多写得极少，是最该缓存的查询。
  - 为什么先删后插：多对多关系（角色↔菜单）没有"原地更新"的概念，全删再全插最简单；@Transactional 保证删了之后插失败能回滚，不会把角色的菜单清空。
- [ ] 4. mapper 模块：新建 `MenuRoleMapper.xml`：`deleteByRid`（delete from menu_role where rid=#{rid}）、`insertRecord`（insert into menu_role(mid,rid) values foreach）
- [ ] 5. web 模块：新建 `PermissController`（controller/system/basic 包，@RequestMapping("/system/basic/permiss")）：`GET /` 角色列表、`GET /menus` 三层菜单树、`GET /mids/{rid}` 回显、`PUT /`（参数 rid + Integer[] mids）保存、`POST /role` 加角色、`DELETE /role/{rid}` 删角色
- [ ] 6. web 模块：新建 `CustomFilterInvocationSecurityMetadataSource`（config 包，@Component，implements FilterInvocationSecurityMetadataSource）：
   - 成员：@Autowired MenuService、`AntPathMatcher antPathMatcher = new AntPathMatcher()`
   - 覆写 `getAttributes`：requestUrl = ((FilterInvocation) object).getRequestUrl() → 拿 getAllMenusWithRole() 列表逐个 antPathMatcher.match(menu.getUrl(), requestUrl) → 命中就把该 menu 的 roles 转成 String[] 返回 SecurityConfig.createList(...) → 全不命中 return SecurityConfig.createList("ROLE_LOGIN")
   - 覆写 getAllConfigAttributes（return null）、supports（return true）
  - 为什么：它回答"这个 url 需要什么角色"。menu.url 存的是 /system/** 这种带通配的模式，所以用 AntPathMatcher 而不是字符串相等。
  - 为什么全不命中返回 ROLE_LOGIN：不在 menu 表里的接口（如 /hr/info、/system/config/menu）不需要特定角色，"登录就能用"，ROLE_LOGIN 是自定义的标记串，交给下面的裁决器识别。
- [ ] 7. web 模块：新建 `CustomUrlDecisionManager`（config 包，@Component，implements AccessDecisionManager）：
   - 覆写 `decide`：遍历 configAttributes；needRole 是 "ROLE_LOGIN" 时，匿名用户（instanceof AnonymousAuthenticationToken）抛 AccessDeniedException("尚未登录，请登录!")，否则 return；其余 needRole 和 authentication.getAuthorities() 逐个 equals，命中 return；循环走完抛 AccessDeniedException("权限不足，请联系管理员!")
   - 覆写两个 supports（return true）
  - 为什么：它回答"当前用户有没有这些角色"。用户有什么来自 Hr.getAuthorities()（阶段 1 写的），需要什么来自上面的 MetadataSource，两边字符串相等即放行；命中一个就 return——"或"的语义。
- [ ] 8. web 模块：SecurityConfig 补阶段 1 留空的部分：
   - 加两个 @Autowired 字段：CustomFilterInvocationSecurityMetadataSource、CustomUrlDecisionManager
   - configure(http) 里 authorizeRequests() 后接 `.withObjectPostProcessor(new ObjectPostProcessor<FilterSecurityInterceptor>() { postProcess 里 object.setAccessDecisionManager(customUrlDecisionManager) + object.setSecurityMetadataSource(customFilterInvocationSecurityMetadataSource) })`
  - 为什么：安全链里真正做"要不要放行"的是 FilterSecurityInterceptor，它默认带着自己的"需要什么角色"和"怎么裁决"两套逻辑；withObjectPostProcessor 是在它初始化完之后把它这两个零件换成我们的两个自定义组件——动态鉴权从此由 menu 表数据驱动，不用再写死在代码里。
- [ ] 9. 自测：权限组页勾选/保存/回显；SQL 插 hr_role 绑测试角色 → 重登录菜单变；先 `redis-cli -a 123456 keys "menus_cache::*"` 看到缓存键（无参 @Cacheable 方法的键名是 `menus_cache::SimpleKey []`），del 掉它，再访问被移除的接口 403；admin 不受影响
  - 为什么改完权限要重登录：Hr 和它的 roles 存在 session 里，是登录那一刻的快照，库里改了 session 不会自己变。
  - 为什么手动清 Redis：getAllMenusWithRole 加了 @Cacheable，库改了缓存里还是旧的角色-菜单关系；vhr 原版没有自动清缓存这一步，教程流程就是手工删。
- [ ] 10. commit：`阶段4：权限组与动态鉴权`

## 阶段 5：操作员管理与个人中心

先看 vhr 这些文件：
- vhr-web：controller/system/HrController.java、controller/HrInfoController.java、config/FastDFSUtils.java
- vhr-service：HrService.java、utils/HrUtils.java
- vhr-mapper：HrMapper.xml、HrRoleMapper.java/.xml

- [ ] 1. service 模块：新建 `HrUtils`（utils 包）：static Hr getCurrentHr()，从 SecurityContextHolder 取 principal 强转返回
  - 为什么：多处要用"当前登录人"，抽个工具方法少写重复的强转代码。
- [ ] 2. mapper 模块：`HrMapper.xml` 补（接口同步）：
   - `HrWithRoles` resultMap（extends BaseResultMap，collection 映射 roles，列别名 rid/rname/rnameZh）+ `getAllHrs(hrid, keywords)`：hr left join hr_role left join role，where hr.id!=#{hrid}，keywords 非空加 name like，order by hr.id
     - 为什么 left join：一个操作员可能还没分配角色，left join 保证他也出现在列表里（roles 为空数组）。
     - 为什么排除自己：操作员列表是"管理别人"的页面，自己出现在里面没有意义还容易误操作。
   - `updateByPrimaryKeySelective`、`deleteByPrimaryKey`、`selectByPrimaryKey`
   - `updateUserface(url, id)`、`updatePasswd(hrid, encodePass)`
- [ ] 3. mapper 模块：新建 `HrRoleMapper.xml`：`deleteByHrid`、`addRole(hrid, rids)`（insert into hr_role(hrid,rid) values foreach）
- [ ] 4. service 模块：`HrService` 补方法：getAllHrs(keywords)（传当前登录人 id）、updateHr、`updateHrRole` @Transactional（先 deleteByHrid 再 addRole，返回 addRole==rids.length）、deleteHrById、updateHrPasswd（new BCryptPasswordEncoder().matches(旧密码, 库里的) 通过才 encode 新密码 updatePasswd）、updateUserface
  - 为什么改密码先 matches 旧密码：证明"知道旧密码的人才能改"，库里存的是密文，比对必须用 encoder.matches 而不是字符串相等（BCrypt 每次加密结果都不同，直接比对永远不等）。
- [ ] 5. web 模块：新建 `HrController`（controller/system 包，/system/hr）：`GET /?keywords=`、`PUT /`、`GET /roles`、`PUT /role`（参数 hrid + Integer[] rids）、`DELETE /{id}`
- [ ] 6. web 模块：新建 `HrInfoController`（controller 包）：
   - `GET /hr/info`：return ((Hr) authentication.getPrincipal())
   - `PUT /hr/info`：updateHr 成功后 new UsernamePasswordAuthenticationToken(hr, authentication.getCredentials(), authentication.getAuthorities()) 塞回 SecurityContextHolder
     - 为什么塞回去：session 里的 principal 还是旧的 Hr，不刷新的话页头名字要重新登录才会变。
   - `PUT /hr/pass`：@RequestBody Map 收 oldpass/pass/hrid，调 updateHrPasswd
   - `POST /hr/userface`：参数 MultipartFile file + Integer id，FastDFSUtils.upload 得 fileId，拼 @Value("${fastdfs.nginx.host}") 更新 userface
- [ ] 7. web 模块：新建 `FastDFSUtils`（config 包）；myhr-web 的 pom 加 `fastdfs-client-java` 1.27.0.0；application.yml 加 `fastdfs.nginx.host`（没搭 FastDFS 就先跳过头像接口）
- [ ] 8. 自测：操作员列表/搜索/启禁/删除；改角色后重登录菜单变；SQL 插 hr + hr_role 建新号（密码存 BCrypt 加密串）→ 登录只见对应菜单；改资料页头刷新；改密码；传头像
- [ ] 9. commit：`阶段5：操作员管理与个人中心`

## 阶段 6：员工管理

先看 vhr 这些文件：
- vhr-web：controller/emp/EmpBasicController.java、converter/DateConverter.java
- vhr-service：EmployeeService.java、NationService.java、PoliticsstatusService.java、utils/POIUtils.java
- vhr-model：Employee.java、Nation.java、Politicsstatus.java
- vhr-mapper：EmployeeMapper、NationMapper、PoliticsstatusMapper（各一个 .java + .xml）

- [ ] 1. `Nation`、`Politicsstatus`：model 模块实体（id/name）+ mapper 全量查询 + service
  - 为什么：员工表单里的民族/政治面貌下拉框要数据源，表就两列，全量查。
- [ ] 2. model 模块：新建 `Employee`：28 个表字段（对照 vhr 的 Base_Column_List）+ nation/politicsstatus/department/jobLevel/position/salary 六个关联对象；每个 Date 字段加 @JsonFormat(pattern="yyyy-MM-dd", timezone="Asia/Shanghai")；全 getter/setter
  - 为什么带关联对象：员工表里存的是 nationId/departmentId 这类外键，但列表页要显示"汉族/研发部"这样的名称，联查时 MyBatis 把关联行填进这些对象，前端直接取 name。
  - 为什么 @JsonFormat：Date 默认序列化成时间戳，前端表格要的是 yyyy-MM-dd 字符串。
- [ ] 3. mapper 模块：新建 `EmployeeMapper.xml`（接口同步，方法参数全部 @Param）：
   - `BaseResultMap`：28 列逐一映射
   - `AllEmployeeInfo`（extends BaseResultMap）：5 个 association——nation（nid/nname）、politicsstatus（pid/pname）、department（did/dname）、jobLevel（jid/jname）、position（posid/posname）
   - `getEmployeeByPage(page, size, emp, beginDateScope)`：employee 与五张表等值联查（别名对应上面的 resultMap）+ if 动态条件（emp.name like、emp.politicId/nationId/departmentId/jobLevelId/posId 等值、emp.engageForm 等值、beginDateScope between）+ 末尾 if page/size 非空加 limit
     - 为什么动态 SQL：高级搜索条件任意组合，用户填了哪个才把哪个拼进 where，一个接口吃下所有组合。
   - `getTotal`：同样的动态条件套 `select count(*) from employee <where>`
     - 为什么条件和列表查询完全一致：total 是前端算总页数的依据，和 data 对不上会出现"第 3 页是空的"或"翻不到最后一页"。
   - `insertSelective`（useGeneratedKeys="true" keyProperty="id"）
     - 为什么 useGeneratedKeys：插入后 MyBatis 把自增 id 回填到 employee 对象上，后续代码（vhr 里是发欢迎邮件查详情）要用这个 id。
   - `addEmps`（values foreach 批量插入）
     - 为什么批量：Excel 导入一次几百行，一条 SQL 插完，别循环单插。
   - `updateByPrimaryKeySelective`、`deleteByPrimaryKey`
   - `maxWorkID`：select max(workID) from employee
   - 接口方法参数全部 @Param
     - 为什么：MyBatis 多参数时不加 @Param，XML 里的 #{emp.name}、#{page} 找不到对应参数。
- [ ] 4. web 模块：新建 `DateConverter`（converter 包）：implements Converter<String, Date>，yyyy-MM-dd 解析，加 @Component
  - 为什么：GET 请求的查询参数（beginDateScope=2020-01-01）默认只会当字符串，Spring 不知道怎么变成 Date，注册这个转换器全局生效。
- [ ] 5. service 模块：新建 `EmployeeService`：
   - `getEmployeeByPage`：page/size 非空先 page=(page-1)*size；查 getEmployeeByPage + getTotal，装进 RespPageBean
     - 为什么 (page-1)*size：前端传的是页码（第几页），SQL 的 limit 要的是偏移量（跳过多少行），第 3 页每页 10 条 = 跳过 20 行。
   - `addEmp`：beginContract/endContract 算 contractTerm（(年差*12+月差)/12，DecimalFormat 保留两位）再 insert
     - 为什么在 Service 算：数据库没有 contractTerm 这一列的来源，它就是由起止日期推出来的冗余值，入库前算好。
   - `maxWorkID`、`updateEmp`、`deleteEmpByEid`、`addEmps`
- [ ] 6. web 模块：新建 `EmpBasicController`（controller/emp 包，/employee/basic）：
   - `GET /`：@RequestParam(defaultValue="1") page、(defaultValue="10") size + Employee 接条件 + Date[] beginDateScope
   - `POST /`、`DELETE /{id}`、`PUT /`
   - `GET /nations`、`/politicsstatus`、`/joblevels`、`/positions`、`/deps`（数据源）
     - 为什么都挂在这个 Controller 下：这五个下拉框只为员工页面服务，跟着员工模块走（vhr 也是这么组织的）。
   - `GET /maxWorkID`：RespBean 的 obj = String.format("%08d", maxWorkID()+1)
     - 为什么补零：工号是 8 位 CHAR（00000001），前端直接回显不用再格式化。
   - `GET /export`、`POST /import`
- [ ] 7. service 模块：新建 `POIUtils`（utils 包）：
   - `employee2Excel(List<Employee>)`：HSSFWorkbook → 文档摘要 → 黄底表头样式 + 日期列样式 → 25 列表头 → 逐行写数据 → ResponseEntity\<byte[]\>（headers 加 Content-Disposition attachment）
   - `excel2Employee(file, nations, politicsstatus, deps, positions, joblevels)`：读 .xls 逐行转 Employee，民族/政治面貌/部门/职位/职称列按名称在传入列表里找 id
     - 为什么按名称找 id：Excel 单元格里是"汉族/研发部"这类文字，库里存的是数字外键，导入时要做一次文字→id 的翻译，所以调用方把全量列表先查好传进来。
- [ ] 8. 自测：分页改每页条数；高级搜索组合条件（total 同步变）；新增（工号补零、合同期限）；编辑、删除；导出→改→导入；脏数据导入提示
- [ ] 9. commit：`阶段6：员工管理`

## 阶段 7：工资账套

先看 vhr 这些文件：
- vhr-web：controller/salary/SalaryController.java、SobConfigController.java
- vhr-service：SalaryService.java
- vhr-model：Salary.java
- vhr-mapper：SalaryMapper.java/.xml、EmployeeMapper.xml（补两个方法）

- [ ] 1. model 模块：新建 `Salary`：14 字段（id、name、basicSalary、bonus、lunchSalary、trafficSalary、allSalary、pensionBase、pensionPer、createDate、medicalBase、medicalPer、accumulationFundBase、accumulationFundPer），createDate 加 @JsonFormat
- [ ] 2. mapper + service：`SalaryMapper`（BaseResultMap + getAllSalaries / insertSelective / deleteByPrimaryKey / updateByPrimaryKeySelective）→ `SalaryService`（addSalary 里 setCreateDate(new Date())）
  - 为什么叫"账套"：工资 = 基本工资+奖金+餐补+交通补+各项社保公积金基数费率，这一组数合起来是一个"套"，不同员工套不同的套。
- [ ] 3. web 模块：新建 `SalaryController`（controller/salary 包，/salary/sob）：`GET /`、`POST /`、`DELETE /{id}`、`PUT /`
- [ ] 4. mapper + service：`EmployeeMapper` 补两个方法（接口 + service 同步）：
   - `EmployeeWithSalary` resultMap（salary association，列别名 sid/sbasicSalary/sbonus/... 全加 s 前缀 + department 的 dname）+ `getEmployeeByPageWithSalary(page, size)`：employee left join empsalary left join salary left join department，order by e.id，if 非空 limit
     - 为什么全部 left join：没分配账套的员工也要出现在列表里（账套列显示为空），用 inner join 他们会整行消失。
     - 为什么列别名加 s 前缀：employee 和 salary 两表都有 id/name/createDate 等同名列，别名区分开 resultMap 才不会串。
   - `updateEmployeeSalaryById(eid, sid)`：`REPLACE INTO empsalary(eid,sid) VALUES(#{eid},#{sid})`
     - 为什么 REPLACE INTO：empsalary 主键是 eid，"改账套"和"第一次分配"是同一条语句——主键存在就删了重插，不存在就插入；对应的，影响行数 1（新插）或 2（替换）都算成功。
- [ ] 5. web 模块：新建 `SobConfigController`（controller/salary 包，/salary/sobcfg）：`GET /?page=&size=`（走 getEmployeeByPageWithSalary，total 用 getTotal(null,null)）、`GET /salaries`、`PUT /`（参数 eid、sid，结果 1 或 2 都算成功）
- [ ] 6. 自测：账套管理页 CRUD；员工账套 tab 分配/改账套；基本资料弹窗改账套联动
- [ ] 7. commit：`阶段7：工资账套`

## 阶段 8：收尾

- [ ] 1. 全功能回归：登录（验证码/顶号/注销）→ 菜单 → 基础信息 → 权限组 → 操作员 → 个人中心 → 员工 → 工资账套
  - 为什么：各阶段互相依赖（改角色影响菜单和 403、删职位影响员工录入），单阶段测过不代表组合起来没问题。
- [ ] 2. 更新 README（功能清单、启动步骤）
- [ ] 3. commit：`阶段8：收尾`
- [ ] 4. 自选扩展：员工高级资料（调岗/调薪/奖惩/培训/离职，表已在库）、注册接口、数据统计图表、接口文档

---

## 暂缓模块（想加回来再做）

> 2026-09-18 决定：在线聊天与邮件服务暂不实现。代码骨架（WebSocket 占位配置、mailserver 模块、amqp/mail 等依赖）全部保留。

### 在线聊天（原阶段 8）

先看 vhr 这些文件：
- vhr-web：config/WebSocketConfig.java、controller/WsController.java、controller/ChatController.java
- vhr-model：ChatMsg.java

- [ ] 1. 重写 `WebSocketConfig`（@EnableWebSocketMessageBroker：端点 /ws/ep + SockJS、simple broker /queue、setAllowedOrigins("http://localhost:10086")；vhr 原文写的是 8080，那是 vhr 前端当时的端口，myhr 换成自己前端的 10086）
  - 为什么 SockJS：浏览器 WebSocket 不通时自动降级到长轮询；setAllowedOrigins 是因为前端和后端端口不同算跨域，不放开握手直接被拒。
- [ ] 2. model 模块：新建 `ChatMsg`（from / fromNickname / to / date / content）
- [ ] 3. web 模块：新建 `WsController`（类上是 @Controller 不是 @RestController；@MessageMapping("/ws/chat") 的方法里从 Authentication 取当前 Hr，补上 chatMsg 的 from/fromNickname/date，再 convertAndSendToUser(to, "/queue/chat", msg)）
  - 为什么 convertAndSendToUser：服务器主动把消息推给"指定用户"订阅的队列，这是聊天"实时送达"的机制（HTTP 只能前端主动问，WebSocket 才能服务器主动推）。
- [ ] 4. web 模块：新建 `ChatController`（GET /chat/hrs）+ service/mapper 补 getAllHrsExceptCurrentHr
- [ ] 5. 验收：两个浏览器不同账号互发消息，实时送达

### 邮件服务（原阶段 9）

先看 vhr 这些文件：
- vhr-service：config/RabbitConfig.java、MailSendLogService.java、task/MailSendTask.java
- mailserver 模块：MailReceiver.java

- [ ] 1. model 模块：新建 `MailConstants`；service 模块：新建 `RabbitConfig`（队列 / 死信交换机 / 绑定声明）
- [ ] 2. mapper + service：`MailSendLogMapper` + `MailSendLogService`
- [ ] 3. `EmployeeService.addEmp`：插入成功后写 mail_send_log（status=0）+ rabbitTemplate.convertAndSend
  - 为什么发消息而不是直接发邮件：发邮件慢（SMTP 几秒），同步发会把新增员工的接口拖卡；丢进 MQ 立即返回，mailserver 慢慢发。
  - 为什么先写 mail_send_log：记下"这条邮件在投递中"，配合下面的定时任务做失败重发。
- [ ] 4. mailserver 模块：`MailReceiver` 消费消息 → Thymeleaf 渲染欢迎邮件 → JavaMailSender 发送 → 更新日志状态
- [ ] 5. service 模块：新建 `MailSendTask`（定时扫描超时/失败记录重发，最多 3 次）
  - 为什么定时任务兜底：MQ 或邮件服务器任何一环挂了消息就丢；日志表里 status 还停在中途的记录就是"疑似丢了"，定时捞出来重发，3 次上限防止死循环。
- [ ] 6. mailserver 的 application.yml 配邮箱 + SMTP 授权码
- [ ] 7. 验收：新增员工收到欢迎邮件；停 mailserver 再新增 → 重启后自动补发
